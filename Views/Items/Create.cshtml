@model RentMate.Models.Item

@{
    ViewData["Title"] = "Nov Predmet";
}

<div class="container py-5">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card shadow-sm border-0">
                <div class="card-header bg-primary text-white">
                    <h4 class="mb-0">Dodaj nov predmet</h4>
                </div>
                <div class="card-body">
                    <form asp-action="Create" enctype="multipart/form-data">
                        <div asp-validation-summary="ModelOnly" class="text-danger"></div>

                        <div class="row mb-3">
                            <div class="col-md-4 text-center">
                                <img id="imgPreview" src="https://via.placeholder.com/200x200?text=Ni+Slike" 
                                     class="img-thumbnail mb-2 object-fit-cover" style="width: 200px; height: 200px;" />
                                <input type="file" name="image" class="form-control form-control-sm" accept="image/*" onchange="previewImage(this)" />
                            </div>
                            <div class="col-md-8">
                                <div class="form-group mb-3">
                                    <label asp-for="Title" class="control-label fw-bold">Naslov</label>
                                    <input asp-for="Title" class="form-control" placeholder="npr. Gorsko kolo" />
                                    <span asp-validation-for="Title" class="text-danger"></span>
                                </div>
                                <div class="row">
                                    <div class="col-md-6 mb-3">
                                        <label asp-for="Category" class="control-label fw-bold">Kategorija</label>
                                        <input asp-for="Category" class="form-control" placeholder="npr. Šport" />
                                        <span asp-validation-for="Category" class="text-danger"></span>
                                    </div>
                                    <div class="col-md-6 mb-3">
                                        <label asp-for="Price" class="control-label fw-bold">Cena na dan (€)</label>
                                        <input asp-for="Price" type="number" step="0.01" class="form-control" />
                                        <span asp-validation-for="Price" class="text-danger"></span>
                                    </div>
                                </div>
                            </div>
                        </div>

                        <div class="form-group mb-4">
                            <label asp-for="Description" class="control-label fw-bold">Opis</label>
                            <textarea asp-for="Description" class="form-control" rows="4"></textarea>
                            <span asp-validation-for="Description" class="text-danger"></span>
                        </div>

                        <div class="d-grid gap-2 d-md-flex justify-content-md-end">
                            <a asp-controller="Dashboard" asp-action="UserDashboard" class="btn btn-secondary me-2">Prekliči</a>
                            <input type="submit" value="Ustvari predmet" class="btn btn-success px-4" />
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
    <script>
        function previewImage(input) {
            if (input.files && input.files[0]) {
                var reader = new FileReader();
                reader.onload = function (e) {
                    document.getElementById('imgPreview').src = e.target.result;
                }
                reader.readAsDataURL(input.files[0]);
            }
        }
    </script>
}